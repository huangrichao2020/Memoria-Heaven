{
  "name": "memoria-heaven-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute memoria-heaven-db --file=./schema.sql",
    "db:init:local": "wrangler d1 execute memoria-heaven-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
